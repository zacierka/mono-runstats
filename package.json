{
  "name": "mono-runstats",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "bot:run": "bun --filter '@apps/bot' run",
    "bot:build": "bun --filter '@apps/bot' build"
  }
}